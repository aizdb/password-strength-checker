# Password Strength Checker 🔐

A simple Python tool to evaluate the strength of a password based on length, digits, uppercase, lowercase, and symbols.

## ✨ Features
- Checks password length (minimum 8 characters)
- Detects digits, uppercase, lowercase, and special symbols
- Returns strength level: Weak, Medium, Strong

## 🚀 Usage
Run the script in your terminal:
```bash
python checker.py
```
## 📌 Example:
Input:

```bash
MyPass123!
```

Output:
```bash
Password strength: Strong
```
